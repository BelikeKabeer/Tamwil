apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-emi
  labels:
    app: backend-emi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-emi
  template:
    metadata:
      labels:
        app: backend-emi
    spec:
      containers:
      - name: backend-emi
        image: 376424942400.dkr.ecr.us-east-1.amazonaws.com/tamwil-backend-emi:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "250Mi"
            cpu: "300m"

---
apiVersion: v1
kind: Service
metadata:
  name: backend-emi
spec:
  type: ClusterIP
  selector:
    app: backend-emi
  ports:
  - port: 5000
    targetPort: 5000